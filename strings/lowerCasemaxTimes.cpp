#include<bits/stdc++.h>
using namespace std;
int main(){
    string s = "leetcode";

    // by using two loops;
    int maxcnt = INT_MIN;
    int cnt = 0;
    for(int i=0;i<s.length();i++){
        for(int j=1;j<=s.length();j++){
            if(s[i] == s[j]){
                cnt++;
                int max
            }
        }
    }
}