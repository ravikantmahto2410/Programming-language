#include<iostream>
#include<vector>
using namespace std;

int main(){
    //this code id self tried but unfortunately  it doesnt run or its wrong correct is twopointer by raghav 
    vector<int>v;
    int n;
    cout<<"Enter the size of array : ";
    cin>>n;

    cout<<"Ente the elements of array : ";
    for(int i=0;i<n;i++){
        int q;
        cin>>q;
        v.push_back(q);
    }

    int i=0;
    int j=v.size()-1;
    for(;i<j;i++){
        for(;j>i;j--){
          if(i>=j){
            b
          }
        }
    }
    
}