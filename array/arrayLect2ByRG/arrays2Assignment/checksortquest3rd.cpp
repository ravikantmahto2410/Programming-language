#include<iostream>
#include<climits>
#include<vector>
using namespace std;

int main(){
    
    int n;
    cout<<"Enter the size of the elements : ";
    cin>>n;
    vector<int>v;
    
    cout<<"Enter the elements of the array : ";
    for(int i=0;i<v.size();i++){
        int q;
        cin>>q;
        v.push_back(q);
    }
      int min=INT_MAX;
      for(int i=0;i<v.size();i++){
        if(q>)
      }
    
    


}